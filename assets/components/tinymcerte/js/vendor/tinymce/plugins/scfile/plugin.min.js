tinymce.PluginManager.add('scfile', function(editor, url) {
    var openDialog = function () {
        return editor.windowManager.open({
            title: 'Добавить шорткод "Виджет документы"',
            body: {
                type: 'panel',
                items: [
                    {
                        type: 'input',
                        name: 'resource',
                        label: 'Список ресурсов'
                    }, {
                        type: 'htmlpanel', // component type
                        html: '<p style="font-size: .75rem; margin-top: .25rem; margin-bottom: 1.25rem;">Список ресурсов, через запятую, для вывода в результатах. Если id товара начинается с минуса, этот товар исключается из выборки.</p>'
                    }, {
                        type: 'input',
                        name: 'limit',
                        label: 'Лимит выборки результатов'
                    }, {
                        type: 'htmlpanel', // component type
                        html: '<p style="font-size: .75rem; margin-top: .25rem; margin-bottom: 1.25rem;">Список ресурсов, через запятую, для вывода в результатах. Если id товара начинается с минуса, этот товар исключается из выборки.</p>'
                    }, {
                        type: 'input',
                        name: 'showSize',
                        label: 'Получать размер файла'
                    }, {
                        type: 'htmlpanel', // component type
                        html: '<p style="font-size: .75rem; margin-top: .25rem; margin-bottom: 1.25rem;">Получать размер файла. Значение 1 - Да, 0 - Нет. По-умолчанию 0</p>'
                    }
                ]
            },
            buttons: [
                {
                    type: 'cancel',
                    text: 'Close'
                },
                {
                    type: 'submit',
                    text: 'Save',
                    primary: true
                }
            ],
            onSubmit: function (api) {

                const data = api.getData();

                const prepare = Object.entries(data)
                    .filter(filter => filter[1] != '')
                    .map(el => {
                        if (el[0] == 'section_title') {
                            return `${el[0]}=/${el[1]}/`;
                        } else {
                            return `${el[0]}=${el[1]}`;
                        }
                    });

                const row = '[file]' + prepare.join(' ') + '[/file]';


                // Insert content when the window form is submitted
                editor.insertContent(row);
                api.close();
            }
        });
    };

    // Add a button that opens a window
    editor.ui.registry.addButton('scfile', {
        text: 'Файлы',
        onAction: function () {
            // Open window
            openDialog();
        }
    });

    // Adds a menu item, which can then be included in any menu via the menu/menubar configuration
    editor.ui.registry.addMenuItem('scfile', {
        text: 'Example plugin',
        onAction: function() {
            // Open window
            openDialog();
        }
    });

    return {
        getMetadata: function () {
            return  {
                name: "Example plugin",
                url: "http://exampleplugindocsurl.com"
            };
        }
    };
});